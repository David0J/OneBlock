name: OneBlock
version: 1.0.0
main: com.oneblock.OneBlockPlugin
api-version: '1.21'
authors: [OpenAI]
description: Per-player OneBlock islands in a void world.
commands:
  ob:
    description: OneBlock commands
    usage: /ob <home|tp|info|reset|reload>
permissions:
  oneblock.admin:
    description: Admin access to OneBlock commands
    default: op
